# photoextractor 0.1.1

* Added `read_img_stamp_date_in_parallel()` to extract timestamps from images using OCR and stamp them into EXIF metadata.
* New dependencies: `tesseract`, `furrr`, and `future`.
* Updated roxygen documentation.
* Example added

# photoextractor 0.1.0

* Initial CRAN submission (fail).

